# Agents

This project uses two agent skills:

**Payload CMS** — `.agents/skills/payload/SKILL.md`  
Start here for collections, fields, hooks, access control, and queries. See `.agents/skills/payload/reference/` for detailed docs.

**Figma → Payload** — `.agents/skills/figma-payload-cms/SKILL.md`  
Use when implementing pages from Figma: MCP discovery, plan doc, phased subagent builds, blocks/globals, and seeding. Example plan: `docs/GLANCE_HOME_PAGE_PLAN.md`.
